<?php
return [
    'enable' => true,

    /**
     * Allow attributes to be used in controller and action.
     * This option can be set the following values:
     * - `false` (bool) means no attributes
     * - `*` or `true` (string | true) means all attributes
     * - `[]` (array) the attribute class names to be allowed
     */
    'attributes' => '*'
];
